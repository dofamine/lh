export const enum ROUTE_QUERY_PARAMS {
  sites = 'sites',
  accounts = 'accounts',
  period = 'period',
}

export const enum ROUTES {
  dashboard = 'dashboard',
  notFound = 'not-found',
  forbidden = 'forbidden',
}
