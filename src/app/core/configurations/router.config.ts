export const enum LH_ROUTE_QUERY_PARAMS {
  sites = 'sites',
  accounts = 'accounts',
  period = 'period',
}

export const enum LH_ROUTES {
  dashboard = 'dashboard',
  notFound = 'not-found',
  forbidden = 'forbidden',
}
